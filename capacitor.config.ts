import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.safehoodtracker.app',
  appName: 'safehood-neighborhood-safety-tracker',
  webDir: 'dist'
};

export default config;
